export enum UserRoles {
  Admin = 'ADMIN',
  Viewer = 'VIEWER',
}

export enum PermissionActions {
  Read = 'READ',
  Write = 'WRITE',
}

export enum PermissionScopes {
  all = '*',
  user = 'USER',
}
